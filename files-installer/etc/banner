OpenWrt x86 Installer

The console broker assigns one installer owner to VGA, ttyS0, or hvc0.
Without console input, VGA starts automatically after 10 seconds.
Manual command:

  owrt-install

The selected target disk will be erased after explicit confirmation.
